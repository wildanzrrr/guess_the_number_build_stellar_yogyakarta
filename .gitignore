# Rust's output directory
target

# Local settings
.soroban
.stellar

# Environment files (secrets / network-specific config)
.env
.env.local
.env.*.local

# Keep the example template tracked
!.env.example
